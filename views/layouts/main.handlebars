<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Mentors</title>

    <link href="https://fonts.googleapis.com/css?family=Open+Sans:400,700" rel="stylesheet">
    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            color: #615F60;
            background-color: #e8e8e8;
            padding: 0;
            margin: 0;
        }
        h1, h2, h3, h4, h5, h6 {
            margin: 0;
        }
        a {
            text-decoration: none;
            color: inherit;
        }
        .long {
            text-overflow: ellipsis;
            white-space: nowrap;
            overflow: hidden;
        }
        .main {
            width: 100%;
            max-width: 1000px;
            margin: auto;
            overflow-x: hidden;
        }
        .mentor-card {
            background-color: #ffffff;
            padding: 15px;
            margin: 15px;
            box-shadow: 3px 3px 10px #d2d2d2;
        }
        .mentor-card .header {
            display: grid;
            grid-template-columns: 4rem auto;
            grid-column-gap: 15px;
            margin-bottom: 25px;
        }
        .mentor-card .header .info {
            overflow: hidden; /* need for ellipsis text */
        }
        .mentor-card .header .profile_image {
            width: 100%;
            align-self: center;
            border-radius: 50%;
        }
        .mentor-card .profile_link:hover {
            color: #FF66A5;
        }
        .mentor-card .tweet {
            white-space: pre-wrap;
        }
        .mentor-card .bio {
            color: #A6A6A6;
            font-size: 0.8rem;
        }
        @media screen and (max-width: 480px) {
            .mentor-card .bio {
                font-size: 0.75rem;
            }
            .mentor-card .tweet {
                font-size: 0.9rem;
            }
            .mentor-card .header {
                grid-template-columns: 3.5rem auto;
            }
            .mentor-card {
                padding: 10px;
                margin: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="main">
        {{{body}}}
    </div>
</body>
</html>
